/** Hostname canonical publik (dengan www). */
export const SITE_PUBLIC_HOSTNAME = "www.aytiindopanel.com";

/** Domain apex — pengunjung dialihkan ke www lewat `next.config` redirects. */
export const SITE_APEX_HOSTNAME = "aytiindopanel.com";

/** Origin HTTPS canonical untuk SEO & CMS default. */
export const SITE_PUBLIC_ORIGIN = `https://${SITE_PUBLIC_HOSTNAME}`;
