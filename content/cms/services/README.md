Katalog layanan utama masih bersumber dari:

- `data/layanan-pages/live.json` (publish CMS)
- `lib/service-pages.ts` (struktur & default konten halaman layanan)

Anda dapat menyimpan **dokumen referensi atau manifest** tambahan di sini (mis. eksport JSON) untuk proses editorial; pembaca aplikasi utama akan ditambahkan jika diperlukan migasi tanpa mengubah URL publik.
